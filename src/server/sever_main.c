#include <stdio.h>
#include "server.h"

void recieve(MessagePacket message)
{
    switch(message.type)
    {
        case: HANDSHAKE_INIT
            //do sth.
            break;

            //.......
    }
}
